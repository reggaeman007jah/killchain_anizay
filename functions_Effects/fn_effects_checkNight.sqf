// this will run a check every so often to see if we should be running a nightlight ops 

sleep 60; // init pause 

while {TRUE} do {
	systemChat "nighttime??";
	// check missiontime 
	// if within a rangge set NIGHTTIME to true, else false 
	// can we use waituntil here?

	sleep 300;
};