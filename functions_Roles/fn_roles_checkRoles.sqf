/*
This will run on mission start, and maybe on each point taken, to know which roles are in-play, and from there 
will generate running scripts that are role-specific 
*/

systemChat "DEBUG / ROLES CHECKER - running roles checker";

{

} forEach allPlayers;